[build]
  # Esse comando obriga a instalação do Vite e corrige a falta de dependencias
  command = "npm install --include=dev && npm run build"
  
  # Pasta de saída
  publish = "dist"
  
  # Pasta das funções
  functions = "netlify/functions"

[build.environment]
  # 🔴 A CURA DO ERRO /dev/null 🔴
  # Isso força o npm a usar uma pasta válida para o cache
  NPM_CONFIG_CACHE = "/opt/build/repo/node_modules/.npm"
  NODE_VERSION = "20"